# Summary: Structures

> Generated by `/summary --knowledge` on 2026-04-06 17:28:00
> Source: `base_knowledge/structures/`
> Purpose: Cung cấp thông tin tổng quan và phase tagging cho `feat-init`. KHÔNG thay thế cho việc đọc file gốc.

## Structures Index

| # | File | Summary | Phase |
|---|------|---------|-------|
| 1 | requirement_structures.md | Định nghĩa danh sách Base Structures (system_overview.md — bắt buộc) và Extend Structures (các service-specific files — tùy chọn theo dự án). File config cho initial-project, không chứa architecture details. | init |

> **Note**: Repo `common-opsx` là framework template — chưa có structures đã reasoning (system_overview.md, *-service.md, etc.). Sau khi chạy `/initial-project` trên dự án cụ thể, bảng này sẽ được cập nhật bằng `/summary --knowledge` với các structures thực tế.

## Phase Legend

- **init**: File chỉ dùng trong `/initial-project` — không cần đọc trong feat-propose/apply
- **propose**: File được đọc trong `feat-propose` — system context, architecture overview
- **apply**: File được đọc trong `feat-apply` — service details, processing flows
- **archive**: File được đọc trong `feat-archive` — documentation sync reference
