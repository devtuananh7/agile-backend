# Summary: Standards & Rules

> Generated by `/summary --knowledge` on 2026-04-06 17:28:00
> Source: `base_knowledge/standards/` + `base_knowledge/common_rules/`
> Purpose: Cung cấp thông tin tổng quan và phase tagging cho `feat-init`. KHÔNG thay thế cho việc đọc file gốc.

## Standards Index

> Source: `base_knowledge/standards/`

| # | File | Summary | Phase |
|---|------|---------|-------|
| 1 | requirement_standards.md | Định nghĩa danh sách Base Requirements (coding, logging, error_handling) và Extend Requirements (dto, database, error_code, security, api, bank_integration, financial_flow, non_financial_flow) cần reasoning từ codebase. File config cho initial-project, không chứa patterns. | init |

> **Note**: Repo `common-opsx` là framework template — chưa có standards đã reasoning (coding_standard.md, logging_standard.md, etc.). Sau khi chạy `/initial-project` trên dự án cụ thể, bảng này sẽ được cập nhật bằng `/summary --knowledge` với các standards thực tế.

## Rules Index

> Source: `base_knowledge/common_rules/`

| # | File | Summary | Phase |
|---|------|---------|-------|
| 1 | rule_planing_feature.md | Quy tắc bắt buộc cho phân tích, thiết kế, planning feature mới. 3 phần: Planning Rules (file structure tree format, 1-1 business mapping, config/error listing, impact analysis), Spec Rules (API contracts, business logic, security scope, edge cases), Design Rules (component interaction sequence diagram, data persistence, concurrency/locking, external integrations). | propose |
| 2 | rule_security.md | Checklist bảo mật bắt buộc cho mọi tính năng. 17 categories: Auth (validate session, hash OTP SHA256), Account (validate ownership hd_valid_less2), Transaction (chặn trùng INCR Redis, validate amount > 0 < MAX, tranToken random), Saving, Loan, Card, QR Payment (hold tiền khi lỗi pha 2), eKYC (cache giấy tờ + IMEI), Key Exchange, Password Expiry, Session Channel Separation. | propose, apply |
| 3 | rule_generate_srs.md | Template và rules cho sinh tài liệu SRS theo IEEE 830. 6 sections: Introduction [REQUIRED], Overall Description [REQUIRED], System Features/Functional Requirements [REQUIRED] (mỗi feature PHẢI có Test Scenarios), External Interface [OPTIONAL], NFRs [OPTIONAL], Other Requirements [OPTIONAL]. Kèm source-to-section mapping từ tracking artifacts. | archive |
| 4 | rule_change_trans_status.md | Nguyên tắc bắt buộc: cập nhật trạng thái giao dịch (Init → In-Process) TRƯỚC KHI xử lý logic nghiệp vụ. Tránh xử lý trùng khi Service restart. Pattern đúng: lấy giao dịch → đổi status ngay → validate → xử lý → update final status (Success/Fail/Timeout). | apply |

## Phase Legend

- **init**: File chỉ dùng trong `/initial-project` — không cần đọc trong feat-propose/apply
- **propose**: File được đọc trong `feat-propose` — thiết kế, planning, API contracts
- **apply**: File được đọc trong `feat-apply` — coding conventions, error handling, logging
- **archive**: File được đọc trong `feat-archive` — SRS generation, documentation sync
